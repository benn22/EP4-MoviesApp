<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_main_icon"
        android:label="CineInnova"
        android:roundIcon="@mipmap/ic_main_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EP4MoviesApp"
        tools:targetApi="31">
        <activity
            android:name="apps.isil.ep4_moviesapp.Activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name="apps.isil.ep4_moviesapp.Activities.DetailActivity"
            android:exported="false" />
        <activity
            android:name="apps.isil.ep4_moviesapp.Activities.MainActivity"
            android:exported="false" />
        <activity
            android:name="apps.isil.ep4_moviesapp.Activities.LoginActivity"
            android:exported="false" />
        <activity
            android:name="apps.isil.ep4_moviesapp.Activities.IntroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>